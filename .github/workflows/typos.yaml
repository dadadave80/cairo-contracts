name: Check for typos

on: [pull_request]

jobs:
  check-for-typos:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v7

      - name: Check for typos
        uses: crate-ci/typos@d43b6c087ac471e2ea7b8af622ff15f05c0c365b
